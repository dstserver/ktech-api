{
  "scripts": {
    "build": "esbuild index.js --bundle --minify --platform=node --outfile=dist/index.js",
    "build:test": "esbuild test.js --bundle --minify --platform=node --outfile=index.test.js",
    "test": "npm run build:test && jest"
  },
  "devDependencies": {
    "esbuild": "latest",
    "express": "latest",
    "supertest": "latest",
    "jest": "latest"
  }
}
