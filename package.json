{
  "scripts": {
    "build": "esbuild index.js --bundle --minify --platform=node --outfile=dist/index.js",
    "build:test": "esbuild test.js --bundle --minify --platform=node --outfile=dist/test.js",
    "test": "npm run build:test && node dist/test.js"
  },
  "devDependencies": {
    "esbuild": "latest",
    "express": "latest",
    "supertest": "latest"
  }
}
